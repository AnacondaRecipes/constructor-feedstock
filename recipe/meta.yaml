{% set version = "3.13.1" %}

package:
  name: constructor
  version: {{ version }}

source:
  - url: https://github.com/conda/constructor/archive/{{ version }}.tar.gz
    sha256: c4c46523dd919eb9c412b4c36b60ff311ac8053bd7d9f3e0a98bdebe58f230c5
    patches:                        # [aarch64]
      - raspberry-pi-warning.patch  # [aarch64]

build:
  number: 0
  script_env:
    - SETUPTOOLS_SCM_PRETEND_VERSION={{ version }}
    - NSIS_USING_LOG_BUILD=1   # [win]
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  entry_points:
    - constructor = constructor.main:main
  skip: true  # [py<38]

requirements:
  host:
    - python
    - pip
    - setuptools
    - setuptools-scm
    - wheel
  run:
    - conda >=4.6
    - python
    - ruamel.yaml >=0.11.14,<0.19
    - conda-standalone
    - pillow >=3.1     # [win or osx]
    - nsis >=3.08      # [win]
    - jinja2
    - jsonschema >=4
  run_constrained:     # [unix]
    - nsis >=3.08      # [unix]
    - conda-libmamba-solver !=24.11.0
    - pydantic >=2.11,<2.12
test:
  source_files:
    - tests
    # Include when running examples
    # - scripts
    # - examples
  requires:
    - pip
    - pytest
    - conda-libmamba-solver
    - nsis =*=*log*  # [win]
    - pywin32        # [win]
  commands:
    - pip check
    - pytest -v tests -k "not examples"
    # Long running integration tests
    # - python scripts/run_examples.py  # [not win]
    - constructor --help
    - constructor -V
  imports:
    - constructor

about:
  home: https://docs.conda.io/
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE.txt
  summary: create installer from conda packages
  description: |
    Constructor is a tool for constructing an installer for a collection of
    conda packages. It creates an Anaconda-like installer consisting of
    packages.
  doc_url: https://docs.conda.io/projects/conda
  dev_url: https://github.com/conda/constructor

extra:
  recipe-maintainers:
    - jakirkham
    - jschueller
    - mbargull
    - msarahan
    - jaimergp
    - marcoesters
